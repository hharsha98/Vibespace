node_modules
dist
build
.DS_Store
*.log
coverage
.vite
.turbo
.env
.env.*
!.env.example
*.db
*.sqlite

# Screenshots pasted into an agent pane (see apps/server/src/files/paste-image.ts).
# These are transient scratch images, not knowledge — unlike `.vibespace/memory/`,
# which is plain markdown deliberately meant to be committed (docs/MEMORY.md).
.vibespace/pastes/
