{
  "name": "vibedeck",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "lint": "eslint . --ignore-pattern \"**/src-tauri/resources/**\" --ignore-pattern \"**/src-tauri/target/**\"",
    "postinstall": "node scripts/fix-native-perms.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "eslint": "^9.19.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.21.0"
  }
}
