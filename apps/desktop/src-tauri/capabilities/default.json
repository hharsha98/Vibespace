{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window/app permissions for vibedeck's main window. Nothing beyond Tauri's own default core set is granted — the served vibedeck app itself talks to its own Node server over plain HTTP/WebSocket, never through Tauri's IPC bridge, so no extra command permissions are needed here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
